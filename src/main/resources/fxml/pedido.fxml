<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.DatePicker?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ListView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<VBox alignment="CENTER" prefHeight="700" prefWidth="800" spacing="15" 
      xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="main.sistemagestionzamira.controller.PedidoController"
      styleClass="contenido-pedido">

    <VBox spacing="5" styleClass="card" prefWidth="400">
        <Label text="👤 Usuario:" styleClass="text-azul"/>
        <TextField fx:id="busquedaUsuarioTextField" promptText="Buscar usuario por nombre..." prefWidth="350" styleClass="input"/>
        <ListView fx:id="resultadosUsuarioListView" prefHeight="100" prefWidth="350"/>
    </VBox>

    <!-- Fecha del pedido -->
    <HBox spacing="10" alignment="CENTER_LEFT" styleClass="card">
        <Label text="📅 Fecha pedido:" styleClass="text-azul"/>
        <DatePicker fx:id="fechaPedidoDatePicker" />
    </HBox>

    <!-- Estado del pedido -->
    <HBox spacing="10" alignment="CENTER_LEFT" styleClass="card">
        <Label text="📋 Estado:" styleClass="text-azul"/>
        <ComboBox fx:id="estadoComboBox" promptText="Selecciona estado" prefWidth="200" styleClass="combo-box"/>
    </HBox>

    <!-- Productos -->
    <VBox spacing="10" styleClass="card" prefWidth="750">
        <Label text="🔍 Buscar y agregar productos:" styleClass="text-azul"/>

        <HBox spacing="10" alignment="CENTER_LEFT">
            <TextField fx:id="busquedaProductoTextField" promptText="Buscar producto..." prefWidth="200" styleClass="input"/>
            <ComboBox fx:id="productoComboBox" promptText="Selecciona producto" prefWidth="200" styleClass="combo-box"/>
            <TextField fx:id="cantidadProductoTextField" promptText="Cantidad" prefWidth="80" />
            <Label fx:id="precioUnitarioLabel" text="Precio Unitario: $" style="-fx-font-weight: bold;"/>
            <Button fx:id="agregarProductoButton" text="Agregar producto" styleClass="button"/>
        </HBox>

        <Label text="🛒 Carrito:" styleClass="text-lila"/>

        <ListView fx:id="productosListView" prefHeight="150" />

        <HBox spacing="10" alignment="CENTER_RIGHT">
            <Label text="Total:" styleClass="text-naranja"/>
            <TextField fx:id="totalTextField" editable="false" prefWidth="120" styleClass="input"/>
        </HBox>
    </VBox>

    <!-- Sección de Pago y Entrega -->
    <VBox spacing="10" styleClass="card" prefWidth="750">
        <Label text="🚚 Entrega" styleClass="text-azul" style="-fx-font-weight: bold; -fx-font-size: 14px;"/>

        <HBox spacing="10" alignment="CENTER_LEFT">
            <Label text="🚚 Método de entrega:" prefWidth="150" styleClass="text-azul"/>
            <ComboBox fx:id="metodoEntregaComboBox" promptText="Selecciona método de entrega" prefWidth="200" styleClass="combo-box"/>
        </HBox>

        <HBox spacing="10" alignment="CENTER_LEFT">
            <Label text="🏠 Dirección (si es envío):" prefWidth="150" styleClass="text-azul"/>
            <TextField fx:id="direccionTextField" promptText="Dirección de envío" prefWidth="350" styleClass="input"/>
        </HBox>

        <HBox spacing="10" alignment="CENTER_LEFT">
            <Label text="📅 Fecha entrega:" prefWidth="150" styleClass="text-azul"/>
            <DatePicker fx:id="fechaEntregaDatePicker" />
        </HBox>
    </VBox>
    
    <VBox spacing="10" styleClass="card" prefWidth="750">
        <Label text="💳 Pago" styleClass="text-azul" style="-fx-font-weight: bold; -fx-font-size: 14px;"/>

        <HBox spacing="10" alignment="CENTER_LEFT">
            <Label text="💳 Método de pago:" prefWidth="150" styleClass="text-azul"/>
            <ComboBox fx:id="metodoPagoComboBox" promptText="Selecciona método de pago" prefWidth="200" styleClass="combo-box"/>
            <TextField fx:id="numeroCuentaTextField" promptText="Número tarjeta o cuenta" prefWidth="200" styleClass="input"/>
        </HBox>

    </VBox>

    <!-- Botón para guardar pedido -->
    <Button fx:id="realizarPedidoButton" text="Realizar Pedido" styleClass="verde button" prefWidth="150" />

    <padding>
        <Insets top="10" bottom="10" left="10" right="10"/>
    </padding>

</VBox>