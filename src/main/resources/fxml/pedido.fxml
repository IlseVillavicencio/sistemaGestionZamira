<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.DatePicker?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ListView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<VBox alignment="CENTER" prefHeight="1000" prefWidth="800" spacing="20.0" styleClass="contenido-pedido" xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="main.sistemagestionzamira.controller.PedidoController">

    <HBox alignment="CENTER" spacing="20">
        <VBox prefHeight="157.0" prefWidth="750.0" spacing="10" styleClass="card">
            <Label styleClass="text-azul" text="Buscar productos" />
            <TextField fx:id="busquedaProductoTextField" promptText="Buscar..." styleClass="input" />
            <ComboBox fx:id="productoComboBox" promptText="Selecciona producto" styleClass="combo-box" />
            <ComboBox promptText="Todas las categorías" styleClass="combo-box" />
            <Button fx:id="agregarProductoButton" text="Agregar producto" />
        </VBox>
    </HBox>

    <VBox fx:id="contenedorProductos" alignment="CENTER" spacing="20.0" />

    <HBox alignment="CENTER" spacing="10">
        <VBox prefHeight="247.0" prefWidth="750.0" spacing="10" styleClass="card">
            <Label styleClass="text-lila" text="🛒 Carrito" />
            <TextField fx:id="clienteTextField" promptText="Nombre del cliente" styleClass="input" />
            <ListView fx:id="productosListView" prefHeight="150" />
            <HBox alignment="CENTER_RIGHT" spacing="10">
                <Button styleClass="amarillo button" text="Seleccionar" />
                <Button fx:id="btnCargarDatos" text="Cargar Datos" onAction="#cargarDatosDePrueba"/>

            </HBox>
        </VBox>
    </HBox>

    <HBox alignment="CENTER" spacing="20">
        <VBox prefHeight="157.0" prefWidth="750.0" spacing="10" styleClass="card">
            <Label styleClass="text-naranja" text="🚚 Método de Entrega" />
            <ComboBox fx:id="metodoEntregaComboBox" promptText="Selecciona entrega" styleClass="combo-box" />
            <TextField fx:id="direccionTextField" promptText="Dirección (si aplica)" styleClass="input" />
            <DatePicker fx:id="fechaEntregaDatePicker" />
            <Button styleClass="verde button" text="Confirmar método de entrega" />
        </VBox>
    </HBox>

          <HBox alignment="CENTER" spacing="20">
        <VBox prefHeight="122.0" prefWidth="750.0" spacing="10" styleClass="card">
            <Label styleClass="text-azul" text="💳 Métodos de Pago" />
            <ComboBox fx:id="metodoPagoComboBox" promptText="Selecciona método de pago" styleClass="combo-box" />
            <TextField fx:id="numeroCuentaTextField" promptText="Número de tarjeta / Cuenta" styleClass="input" />
            <Button fx:id="realizarPedidoButton" styleClass="verde button" text="Finalizar pago" />
        </VBox>
    </HBox>

    <padding>
        <Insets bottom="8.0" left="8.0" right="8.0" top="8.0" />
    </padding>
</VBox>
